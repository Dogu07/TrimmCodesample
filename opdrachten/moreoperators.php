<?php

echo <<<UITLEG
<h2>Ternary operator</h2>
<p>
Ternary operator is gebruik van een logische process zoals condition of true return value : false return value. statements om je if else structuur te verkleinen.
</p>

<br />
<h2>Dot operator</h2>
<p>De dot is een tekst samenvoeger. het helpt je met samen voegen van 2 teksten.</p>

<h2>incrementing/decrementing operators</h2>
<p>je hebt verschillende incrementing en decrementing operators</p>
<p>Zoals : ++a	Pre-increment = Increments a by one, then returns a.</p>
<p>Zoals : a++	post-increment =Returns a, then increments a by one..</p>
<p>Zoals : --a Pre-Decrement = Decrements a by one, then returns a.</p>
<p>Zoals : a-- Post-Decrement = Returns a, then decrements a by one.</p>

<h2>Verschil tussen ++ en --</h2>
<p>++ doet +1</p>
<p>-- doet -1</p>

<h2>Assignment Operators</h2>
<p>De operator voor de basistaak is een "=". Je eerste neiging zou kunnen zijn om dit te zien als een "gelijk aan". Niet doen want 
Het betekent echt dat de linker operator wordt ingesteld op de waarde van de uitdrukking aan de rechterkant (dat wil zeggen: "wordt ingesteld op").</p>

<p>Assigment operators zijn soort van Shorthands</p>


UITLEG;
?>